# The app itself uses Python's standard library HTTP server.
# These packages are only needed for document extraction/export and sample generation.
pdfplumber>=0.11,<1
python-docx>=1.1,<2
reportlab>=4.2,<5

